---
- name: Deploy web container using Docker
  hosts: web
  gather_facts: false

  tasks:
    - name: Remove old container if exists
      shell: "sudo docker rm -f devops-web"
      ignore_errors: yes

    - name: Run new container
      shell: "sudo docker run -d -p 8091:80 --name devops-web devops-web:latest"