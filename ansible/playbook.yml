- name: Deploy web contrainer using Docker
  hosts: web
  gather_facts: false 

  tasks:
    - name: Force remove old container if exists
      shell: docker rm -f devops-web || true

    - name: Run new container
      shell: docker run -d -p 8091:80 --name devops-web devops-web:latest